import flet as ft

class TaskComponen:
    def __init__(self):
        pass

    def build():
        return ft.View(
            "/config",
            controls = [
                ft.TextField(password=True)
            ]
         )